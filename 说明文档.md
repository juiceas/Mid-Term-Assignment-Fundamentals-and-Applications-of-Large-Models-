# Transformer项目简单说明文档

## 这个项目是什么？

这是一个**从零开始实现的Transformer模型**，包括完整的Encoder和Decoder。这是为了大模型课程期中作业而创建的项目。

## 我可以用它做什么？

1. **学习Transformer的工作原理**：代码中有详细的注释和说明
2. **训练自己的模型**：在小数据集上快速训练和测试
3. **做实验**：通过消融实验了解不同组件的作用
4. **修改和扩展**：代码结构清晰，容易修改和扩展

## 快速开始（3步）

### 第1步：安装依赖

打开命令行，运行：

```bash
pip install -r requirements.txt
```

这会安装PyTorch、numpy、matplotlib等必要的库。

### 第2步：测试是否安装成功

Windows用户：
```bash
scripts\run.bat test
```

Linux/Mac用户：
```bash
./scripts/run.sh test
```

如果看到"模型测试成功！"，说明安装正确。

### 第3步：开始训练

Windows用户：
```bash
scripts\run.bat train
```

Linux/Mac用户：
```bash
./scripts/run.sh train
```

训练会自动开始，你会看到进度条和损失值。

## 训练完成后在哪里找结果？

训练完成后，会自动生成以下文件：

1. **训练曲线图**：`results/training_curves.png`
   - 包含4个图：训练损失、验证损失、困惑度、学习率变化

2. **训练历史数据**：`results/training_history.json`
   - 包含每个epoch的详细数据（JSON格式）

3. **最佳模型**：`checkpoints/best_model.pth`
   - 验证损失最低的模型

## 如何做消融实验？

消融实验可以帮助你理解每个组件的作用。

运行消融实验：

Windows用户：
```bash
scripts\run.bat ablation
```

Linux/Mac用户：
```bash
./scripts/run.sh ablation
```

实验会自动测试：
- 不同的注意力头数（2, 4, 8）
- 不同的层数（2, 4, 6）
- 不同的模型大小（128, 256, 512维度）
- 不同的Dropout率（0.0, 0.1, 0.3）
- 不同的优化器（Adam, AdamW）

结果保存在：`results/ablation_study/`

## 各个文件的作用

### 源代码（src/文件夹）

| 文件 | 作用 | 需要修改吗？ |
|-----|------|------------|
| `model.py` | Transformer模型的实现 | ❌ 通常不需要 |
| `data_loader.py` | 数据加载和预处理 | ⚠️ 如果要用自己的数据，需要修改 |
| `train.py` | 训练脚本 | ✅ 可以修改配置参数 |
| `ablation_study.py` | 消融实验脚本 | ⚠️ 可以添加新的实验 |

### 配置文件（configs/文件夹）

| 文件 | 用途 |
|-----|------|
| `base.yaml` | 基础配置（平衡性能和速度） |
| `small.yaml` | 小模型配置（快速测试） |
| `large.yaml` | 大模型配置（追求性能） |

### 运行脚本（scripts/文件夹）

| 文件 | 用途 |
|-----|------|
| `run.sh` | Linux/Mac运行脚本 |
| `run.bat` | Windows运行脚本 |

## 常见问题解答

### ❓ 训练需要多长时间？

在普通笔记本电脑上（CPU）：
- 小模型（small.yaml）：约10分钟
- 基础模型（base.yaml）：约30分钟
- 大模型（large.yaml）：约1小时

如果有GPU，速度会快5-10倍。

### ❓ 需要什么配置的电脑？

**最低要求**：
- 任何能运行Python的电脑
- 4GB内存
- 500MB硬盘空间

**推荐配置**：
- 8GB或更多内存
- 有NVIDIA GPU（可选）

### ❓ 内存不足怎么办？

修改`src/train.py`文件中的配置：

```python
config = {
    'batch_size': 16,    # 改小（原来是32）
    'd_model': 128,      # 改小（原来是256）
    'num_samples': 1000, # 改小（原来是2000）
}
```

### ❓ 怎么知道训练进展如何？

训练时会显示：
```
Epoch 10/50 [Train]: 100%|████████| 50/50 [00:15<00:00]
  Train Loss: 0.5234, Perplexity: 1.6877
  Val Loss: 0.6123, Perplexity: 1.8443
```

- **Loss（损失）越小越好**
- **Perplexity（困惑度）越小越好**
- 如果Loss不再下降，可以提前停止

### ❓ 如何使用自己的数据？

1. 修改`src/data_loader.py`中的数据加载函数
2. 准备数据格式：
   - 翻译任务：两个文本文件，每行对应
   - 语言模型：一个文本文件，每行一个句子

3. 在`train.py`中调用新的数据加载函数

### ❓ 训练中断了怎么办？

不用担心！代码会自动保存检查点。

找到最新的检查点文件（在`checkpoints/`文件夹），然后在代码中加载：

```python
trainer.load_checkpoint('checkpoints/checkpoint_epoch_40.pth')
```

### ❓ 如何调整超参数？

打开`src/train.py`，找到config字典：

```python
config = {
    'd_model': 256,      # 模型维度（越大模型越强但越慢）
    'n_heads': 8,        # 注意力头数（通常是4或8）
    'n_layers': 4,       # 层数（越多越强但越慢）
    'batch_size': 32,    # 批大小（越大训练越快但需要更多内存）
    'lr': 3e-4,         # 学习率（太大不收敛，太小训练慢）
    'epochs': 50,        # 训练轮数
}
```

**建议**：
- 初学者：使用`small.yaml`的配置
- 一般使用：使用`base.yaml`的配置
- 追求性能：使用`large.yaml`的配置

## 项目亮点

✅ **完整实现**：不仅有Encoder，还有Decoder  
✅ **代码清晰**：每个函数都有注释说明  
✅ **易于使用**：一键运行，自动保存结果  
✅ **功能完善**：包括训练、验证、保存、可视化  
✅ **可扩展**：容易修改和添加新功能  

## 学习路线

如果你想深入理解代码，建议按以下顺序阅读：

1. **先看模型结构**：`src/model.py`
   - 从简单的开始：`PositionalEncoding` → `ScaledDotProductAttention`
   - 然后看复杂的：`MultiHeadAttention` → `EncoderLayer` → `DecoderLayer`
   - 最后看完整模型：`Transformer`

2. **再看数据处理**：`src/data_loader.py`
   - 了解数据如何加载和预处理

3. **最后看训练流程**：`src/train.py`
   - 了解训练循环、优化器、损失函数等

## 代码示例

### 创建一个小模型

```python
from src.model import Transformer

model = Transformer(
    src_vocab_size=1000,   # 源语言词汇表大小
    tgt_vocab_size=1000,   # 目标语言词汇表大小
    d_model=128,           # 模型维度
    n_heads=4,             # 注意力头数
    d_ff=512,             # 前馈网络维度
    n_layers=2,           # 层数
    max_len=50,           # 最大序列长度
    dropout=0.1           # Dropout率
)

# 查看模型参数数量
total_params = sum(p.numel() for p in model.parameters())
print(f"参数数量: {total_params:,}")
```

### 加载训练好的模型

```python
import torch
from src.model import Transformer

# 创建模型（参数要和训练时一致）
model = Transformer(...)

# 加载权重
checkpoint = torch.load('checkpoints/best_model.pth')
model.load_state_dict(checkpoint['model_state_dict'])

# 切换到评估模式
model.eval()
```

## 遇到问题？

如果遇到任何问题，请检查：

1. **Python版本**：建议使用Python 3.8或更高版本
2. **依赖安装**：确保`pip install -r requirements.txt`成功
3. **PyTorch安装**：运行`python -c "import torch; print(torch.__version__)"`检查
4. **内存**：如果内存不足，减小batch_size或模型大小
5. **路径**：确保在项目根目录运行命令

## 下一步

完成基础训练后，你可以：

1. ✅ 运行消融实验，了解各组件的作用
2. ✅ 修改超参数，观察对性能的影响
3. ✅ 尝试自己的数据集
4. ✅ 添加新功能（例如：beam search、温度采样）
5. ✅ 可视化注意力权重
6. ✅ 实现相对位置编码

## 总结

这个项目提供了一个**完整、清晰、易用**的Transformer实现。无论你是想学习Transformer的原理，还是想做实验研究，都可以使用这个项目作为起点。

**记住**：
- 从小模型开始
- 一步一步来
- 遇到问题查看文档
- 实验是最好的学习方式

祝你使用愉快！🚀

